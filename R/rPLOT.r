#library(ggplot2)
data <- read.csv("csv/Data.csv",header=T,sep=",") # No need for header and sep though
Roll_No <- data[,1]
# qplot(data$Programming) 
# hist
#Error in qplot(data$Programming) : could no
#Execution halted
#print(a <- require(ggplot2))
